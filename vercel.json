{
  "framework": "nextjs",
  "buildCommand": "cd shared && npm install && npm run build && cd ../frontend && npm install && npm run build",
  "outputDirectory": "frontend/.next",
  "installCommand": "cd shared && npm install && cd ../frontend && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/frontend/$1"
    }
  ]
}
